<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AMRAPP" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Background color -->
        <item name="android:windowBackground">@color/primary_light</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">#2ac99a</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:textColor">@color/secondary_dark</item>
        <item name="android:textColorPrimary">@color/secondary_dark</item>
        <item name="fontFamily">sans-serif-medium</item>

        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorPrimarySurface">@color/black</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorSwitchThumbNormal">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>-->
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_dark</item>
        <!-- Customize your theme here. -->

        <!--
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorBackground">@color/primary_light</item>
        <item name="android:colorButtonNormal">@color/primary_dark</item>
        <item name="android:textColor">@color/secondary_dark</item>
        -->
    </style>
</resources>